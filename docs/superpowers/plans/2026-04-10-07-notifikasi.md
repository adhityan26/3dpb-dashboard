# Shopee Dashboard — Plan 7: Notifikasi

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans.

**Goal:** Sistem notifikasi otomatis yang mendeteksi 4 trigger (order numpuk, stok kritis, ROAS turun drastis, produk delist), kirim alert ke Telegram dan/atau Pushover berdasarkan config dari Plan 6, dengan deduplication agar alert yang sama tidak dikirim berulang.

**Architecture:** Sender layer untuk Telegram dan Pushover (pure functions, no state). Detector layer yang memeriksa setiap kategori alert dan return list event yang perlu dinotifikasi. Background polling endpoint (`/api/notifications/poll`) yang dipanggil oleh internal cron job (Next.js startup setInterval) atau bisa di-trigger manual via API. Deduplication via `NotificationLog` Prisma table — alert dengan key+date yang sama tidak dikirim ulang dalam window tertentu.

**Tech Stack:** Next.js 16 (App Router + global init via `instrumentation.ts`), Prisma 7 (`NotificationLog` model baru), existing Plan 6 sender functions.

**Dependencies:** Plan 1-6 selesai. Khususnya butuh:
- `NotificationConfig` & `AlertThresholds` dari Plan 6
- `getReadyToShipOrders` (Plan 2) untuk deteksi order numpuk
- `getProducts` (Plan 5) untuk deteksi stok kritis & produk delist
- `getAdsPerformance` (Plan 3) untuk deteksi ROAS turun

---

## Trigger Definitions

| # | Event | Detector logic | Severity |
|---|-------|---------------|---------|
| 1 | Order numpuk | Ada ≥ `orderPileupCount` order belum cetak yang umurnya > `orderPileupHours` jam | warning |
| 2 | Stok kritis | Ada produk dengan stok varian < `stockMin`, dedup per `productId+variantId`, dikirim sekali per 24 jam | warning |
| 3 | ROAS drop | Iklan yang sebelumnya 7-day ROAS ≥ 5x, sekarang ROAS-nya < `roasMin` (default 2x). Dedup per `campaignId`, kirim sekali per 24 jam | high |
| 4 | Produk delist | Status produk berubah dari NORMAL ke BANNED/DELETED (dibanding snapshot terakhir di DB) | critical |

**Catatan:** Untuk trigger #3 (ROAS drop), kita butuh historical baseline. Plan 7 tidak track historical secara persisten — kita ambil 2 perhitungan: ROAS 7 hari terakhir vs ROAS 1 hari terakhir. Kalau drop signifikan dalam 1 hari, alert dikirim. Lebih sederhana dari time-series proper dan cukup untuk MVP.

**Catatan trigger #4:** Butuh "snapshot status produk" sebelumnya. Plan 7 simpan snapshot terakhir di tabel baru `ProductStatusSnapshot` (key-value).

---

## Deduplication Strategy

`NotificationLog` table:
```prisma
model NotificationLog {
  id        String   @id @default(cuid())
  alertKey  String   // unique identifier per alert (e.g., "stock_low:item123:variantABC")
  channel   String   // "telegram" | "pushover"
  sentAt    DateTime @default(now())
  message   String

  @@index([alertKey, sentAt])
}
```

Sebelum kirim, detector check: apakah ada `NotificationLog` dengan `alertKey` yang sama dalam 24 jam terakhir? Kalau ada → skip. Kalau tidak → kirim & log.

Untuk order pile-up, dedup window cukup 1 jam (lebih cepat).

---

## File Structure

```
shopee-dashboard/
├── prisma/
│   └── schema.prisma                # MODIFY: add NotificationLog + ProductStatusSnapshot
├── lib/
│   └── notifications/
│       ├── types.ts                 # NEW: AlertEvent type
│       ├── senders.ts               # NEW: sendTelegram, sendPushover (with config from DB)
│       ├── dedupe.ts                # NEW: hasRecentLog, logSent
│       ├── detectors/
│       │   ├── order-pileup.ts      # NEW
│       │   ├── stock-low.ts         # NEW
│       │   ├── roas-drop.ts         # NEW
│       │   └── product-delist.ts    # NEW
│       └── runner.ts                # NEW: runAllDetectors() — main entry
├── app/
│   ├── api/
│   │   └── notifications/
│   │       └── poll/
│   │           └── route.ts         # NEW: POST trigger detector run
│   └── (dashboard)/
│       └── settings/
│           └── page.tsx             # MODIFY: add "Run Check Now" button
├── instrumentation.ts               # NEW: startup hook for background interval
└── components/
    └── settings/
        └── NotificationRunnerCard.tsx  # NEW: manual trigger + last run status
```

---

## Task 1: Prisma Schema Updates

**Files:**
- Modify: `prisma/schema.prisma`
- Run: migration

- [ ] **Step 1: Add models**

Append to `prisma/schema.prisma`:

```prisma
model NotificationLog {
  id        String   @id @default(cuid())
  alertKey  String
  channel   String
  sentAt    DateTime @default(now())
  message   String

  @@index([alertKey, sentAt])
}

model ProductStatusSnapshot {
  productId String   @id
  status    String
  updatedAt DateTime @updatedAt
}
```

- [ ] **Step 2: Run migration**

```bash
cd /Users/adhityatangahu/Documents/shopee-analysis/shopee-dashboard
source ~/.nvm/nvm.sh && nvm use 22
npx prisma migrate dev --name add_notification_log_and_product_snapshot
```

Expected: migration applied, Prisma client regenerated.

- [ ] **Step 3: Verify tables**

```bash
sqlite3 dev.db ".tables"
```

Expected: include `NotificationLog`, `ProductStatusSnapshot`.

- [ ] **Step 4: Commit**

```bash
git add prisma/schema.prisma prisma/migrations/
git commit -m "feat(prisma): add NotificationLog and ProductStatusSnapshot models"
```

---

## Task 2: Notification Types + Senders

**Files:**
- Create: `lib/notifications/types.ts`
- Create: `lib/notifications/senders.ts`

- [ ] **Step 1: Create types**

```typescript
// lib/notifications/types.ts

export type AlertSeverity = "info" | "warning" | "high" | "critical"
export type AlertKind =
  | "order_pileup"
  | "stock_low"
  | "roas_drop"
  | "product_delist"

export interface AlertEvent {
  kind: AlertKind
  severity: AlertSeverity
  /** Stable identifier for dedup within a 24h window */
  alertKey: string
  title: string
  body: string
  /** Dedup window in hours. Defaults to 24. Use shorter for time-sensitive alerts. */
  dedupHours?: number
}

export interface RunnerResult {
  detected: number
  sent: number
  skipped: number // skipped due to dedup
  failed: number
  events: Array<{
    alertKey: string
    kind: AlertKind
    sent: boolean
    reason?: string
  }>
  ranAt: string
}
```

- [ ] **Step 2: Create senders**

```typescript
// lib/notifications/senders.ts

import { getNotificationConfig } from "@/lib/settings/service"
import { prisma } from "@/lib/db"
import type { AlertEvent } from "./types"

interface SendResult {
  ok: boolean
  channel: "telegram" | "pushover"
  error?: string
}

function formatMessage(event: AlertEvent): string {
  const icon = severityIcon(event.severity)
  return `${icon} *${event.title}*\n\n${event.body}`
}

function severityIcon(severity: AlertEvent["severity"]): string {
  switch (severity) {
    case "critical":
      return "🚨"
    case "high":
      return "⚠️"
    case "warning":
      return "🟡"
    default:
      return "ℹ️"
  }
}

export async function sendToTelegram(event: AlertEvent): Promise<SendResult> {
  const { telegramBotToken, telegramChatId } = await getNotificationConfig()
  if (!telegramBotToken || !telegramChatId) {
    return {
      ok: false,
      channel: "telegram",
      error: "Telegram not configured",
    }
  }

  try {
    const res = await fetch(
      `https://api.telegram.org/bot${telegramBotToken}/sendMessage`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          chat_id: telegramChatId,
          text: formatMessage(event),
          parse_mode: "Markdown",
        }),
      },
    )
    const json = (await res.json()) as { ok?: boolean; description?: string }
    if (!json.ok) {
      return {
        ok: false,
        channel: "telegram",
        error: json.description ?? `HTTP ${res.status}`,
      }
    }

    await prisma.notificationLog.create({
      data: {
        alertKey: event.alertKey,
        channel: "telegram",
        message: `${event.title}: ${event.body}`,
      },
    })
    return { ok: true, channel: "telegram" }
  } catch (err) {
    return {
      ok: false,
      channel: "telegram",
      error: err instanceof Error ? err.message : "Network error",
    }
  }
}

export async function sendToPushover(event: AlertEvent): Promise<SendResult> {
  const { pushoverUserKey, pushoverAppToken } = await getNotificationConfig()
  if (!pushoverUserKey || !pushoverAppToken) {
    return {
      ok: false,
      channel: "pushover",
      error: "Pushover not configured",
    }
  }

  // Pushover priority: -2..2. Map severity to priority.
  const priorityMap: Record<AlertEvent["severity"], number> = {
    info: -1,
    warning: 0,
    high: 1,
    critical: 2,
  }

  try {
    const body = new URLSearchParams({
      token: pushoverAppToken,
      user: pushoverUserKey,
      title: `${severityIcon(event.severity)} ${event.title}`,
      message: event.body,
      priority: String(priorityMap[event.severity]),
      // Critical priority requires retry/expire params
      ...(event.severity === "critical"
        ? { retry: "60", expire: "3600" }
        : {}),
    })

    const res = await fetch("https://api.pushover.net/1/messages.json", {
      method: "POST",
      headers: { "Content-Type": "application/x-www-form-urlencoded" },
      body: body.toString(),
    })
    const json = (await res.json()) as {
      status?: number
      errors?: string[]
    }
    if (json.status !== 1) {
      return {
        ok: false,
        channel: "pushover",
        error: json.errors?.join(", ") ?? `HTTP ${res.status}`,
      }
    }

    await prisma.notificationLog.create({
      data: {
        alertKey: event.alertKey,
        channel: "pushover",
        message: `${event.title}: ${event.body}`,
      },
    })
    return { ok: true, channel: "pushover" }
  } catch (err) {
    return {
      ok: false,
      channel: "pushover",
      error: err instanceof Error ? err.message : "Network error",
    }
  }
}

/**
 * Send to all configured channels in parallel.
 * Returns true if at least one channel succeeded.
 */
export async function sendAlert(event: AlertEvent): Promise<{
  anySent: boolean
  results: SendResult[]
}> {
  const results = await Promise.all([
    sendToTelegram(event),
    sendToPushover(event),
  ])
  return {
    anySent: results.some((r) => r.ok),
    results,
  }
}
```

- [ ] **Step 3: Verify build & commit**

```bash
npm run build 2>&1 | tail -10
git add lib/notifications/types.ts lib/notifications/senders.ts
git commit -m "feat(notifications): add types and senders for telegram + pushover"
```

---

## Task 3: Dedupe Helper

**Files:**
- Create: `lib/notifications/dedupe.ts`

- [ ] **Step 1: Create helper**

```typescript
// lib/notifications/dedupe.ts

import { prisma } from "@/lib/db"

const DEFAULT_DEDUP_HOURS = 24

/**
 * Check if an alert with the given key was already sent within the dedup window.
 * Returns true if it was sent (caller should skip), false if not (caller should send).
 */
export async function wasRecentlySent(
  alertKey: string,
  dedupHours: number = DEFAULT_DEDUP_HOURS,
): Promise<boolean> {
  const cutoff = new Date(Date.now() - dedupHours * 60 * 60 * 1000)
  const recent = await prisma.notificationLog.findFirst({
    where: {
      alertKey,
      sentAt: { gte: cutoff },
    },
    orderBy: { sentAt: "desc" },
  })
  return recent !== null
}

/**
 * Cleanup old notification logs (older than 30 days). Optional housekeeping.
 */
export async function pruneOldLogs(): Promise<number> {
  const cutoff = new Date(Date.now() - 30 * 24 * 60 * 60 * 1000)
  const result = await prisma.notificationLog.deleteMany({
    where: { sentAt: { lt: cutoff } },
  })
  return result.count
}
```

- [ ] **Step 2: Verify build & commit**

```bash
npm run build 2>&1 | tail -10
git add lib/notifications/dedupe.ts
git commit -m "feat(notifications): add dedupe helper for notification log"
```

---

## Task 4: Detectors

**Files:**
- Create: `lib/notifications/detectors/order-pileup.ts`
- Create: `lib/notifications/detectors/stock-low.ts`
- Create: `lib/notifications/detectors/roas-drop.ts`
- Create: `lib/notifications/detectors/product-delist.ts`

### Step 1: Order Pile-up Detector

Create `lib/notifications/detectors/order-pileup.ts`:

```typescript
import { getReadyToShipOrders } from "@/lib/orders/service"
import { getAlertThresholds } from "@/lib/settings/service"
import type { AlertEvent } from "../types"

/**
 * Detect orders that have been READY_TO_SHIP without label print for too long.
 * Returns one alert event if threshold exceeded.
 */
export async function detectOrderPileup(): Promise<AlertEvent[]> {
  const thresholds = await getAlertThresholds()
  const result = await getReadyToShipOrders()

  const cutoff = Math.floor(Date.now() / 1000) - thresholds.orderPileupHours * 3600
  const stale = result.orders.filter(
    (o) => !o.labelPrinted && o.createTime <= cutoff,
  )

  if (stale.length < thresholds.orderPileupCount) {
    return []
  }

  // Use date-bucket so the same condition only fires once per hour-of-day
  const hourBucket = new Date()
  hourBucket.setMinutes(0, 0, 0)
  const alertKey = `order_pileup:${hourBucket.toISOString()}`

  return [
    {
      kind: "order_pileup",
      severity: "warning",
      alertKey,
      title: "Order Numpuk!",
      body: `${stale.length} order belum dicetak labelnya lebih dari ${thresholds.orderPileupHours} jam. Cek tab Order sekarang.`,
      dedupHours: 1,
    },
  ]
}
```

### Step 2: Stock Low Detector

Create `lib/notifications/detectors/stock-low.ts`:

```typescript
import { getProducts } from "@/lib/products/service"
import { getAlertThresholds } from "@/lib/settings/service"
import type { AlertEvent } from "../types"

export async function detectStockLow(): Promise<AlertEvent[]> {
  const thresholds = await getAlertThresholds()
  const result = await getProducts()

  const events: AlertEvent[] = []

  for (const product of result.products) {
    if (product.status !== "NORMAL") continue

    if (product.hasVariants) {
      // Per-variant alerts
      for (const variant of product.variants) {
        if (variant.stock < thresholds.stockMin) {
          events.push({
            kind: "stock_low",
            severity: "warning",
            alertKey: `stock_low:${product.productId}:${variant.variantId}`,
            title: "Stok Hampir Habis",
            body: `${product.name} — varian "${variant.variantName}" tinggal ${variant.stock} pcs (threshold: ${thresholds.stockMin})`,
          })
        }
      }
    } else {
      if (product.stockTotal < thresholds.stockMin) {
        events.push({
          kind: "stock_low",
          severity: "warning",
          alertKey: `stock_low:${product.productId}`,
          title: "Stok Hampir Habis",
          body: `${product.name} tinggal ${product.stockTotal} pcs (threshold: ${thresholds.stockMin})`,
        })
      }
    }
  }

  return events
}
```

### Step 3: ROAS Drop Detector

Create `lib/notifications/detectors/roas-drop.ts`:

```typescript
import { getAdsPerformance } from "@/lib/ads/service"
import { getAlertThresholds } from "@/lib/settings/service"
import type { AlertEvent } from "../types"

/**
 * Detect ads where 7-day ROAS was previously good but recent (1-day-effective)
 * has dropped below the threshold.
 *
 * We approximate "recent" by comparing the daily series:
 * - Historical avg ROAS: average of first 6 days in the 7-day window
 * - Recent ROAS: last day's ROAS
 * If historical ≥ 5x AND recent < threshold AND historical / recent ratio ≥ 3,
 * fire an alert.
 */
export async function detectRoasDrop(): Promise<AlertEvent[]> {
  const thresholds = await getAlertThresholds()
  const result = await getAdsPerformance("7d")

  const events: AlertEvent[] = []

  for (const ad of result.ads) {
    if (ad.status !== "berjalan") continue
    if (ad.daily.length < 4) continue

    const days = ad.daily
    const recentDay = days[days.length - 1]
    const historical = days.slice(0, -1)

    const histAvgRoas =
      historical.reduce((s, d) => s + d.roas, 0) / historical.length
    const recentRoas = recentDay.roas

    if (
      histAvgRoas >= 5 &&
      recentRoas < thresholds.roasMin &&
      histAvgRoas / Math.max(recentRoas, 0.01) >= 3
    ) {
      events.push({
        kind: "roas_drop",
        severity: "high",
        alertKey: `roas_drop:${ad.campaignId}`,
        title: "ROAS Iklan Turun Drastis",
        body: `${ad.adName} — ROAS turun dari ${histAvgRoas.toFixed(1)}x (rata-rata 6 hari) ke ${recentRoas.toFixed(1)}x kemarin. Threshold: ${thresholds.roasMin}x.`,
      })
    }
  }

  return events
}
```

### Step 4: Product Delist Detector

Create `lib/notifications/detectors/product-delist.ts`:

```typescript
import { prisma } from "@/lib/db"
import { getProducts } from "@/lib/products/service"
import type { AlertEvent } from "../types"

/**
 * Detect products whose status changed from NORMAL to BANNED/DELETED since
 * the last snapshot.
 */
export async function detectProductDelist(): Promise<AlertEvent[]> {
  const result = await getProducts()
  const snapshots = await prisma.productStatusSnapshot.findMany()
  const snapshotMap = new Map(snapshots.map((s) => [s.productId, s.status]))

  const events: AlertEvent[] = []
  const upserts: Array<Promise<unknown>> = []

  for (const product of result.products) {
    const prevStatus = snapshotMap.get(product.productId)
    const currStatus = product.status

    // First time we see this product → just snapshot, no alert
    if (prevStatus === undefined) {
      upserts.push(
        prisma.productStatusSnapshot.create({
          data: { productId: product.productId, status: currStatus },
        }),
      )
      continue
    }

    if (prevStatus !== currStatus) {
      // Status changed — update snapshot
      upserts.push(
        prisma.productStatusSnapshot.update({
          where: { productId: product.productId },
          data: { status: currStatus },
        }),
      )

      // Alert only on transitions to BANNED or DELETED from NORMAL
      const dangerStatuses = new Set(["BANNED", "DELETED"])
      if (prevStatus === "NORMAL" && dangerStatuses.has(currStatus)) {
        events.push({
          kind: "product_delist",
          severity: "critical",
          alertKey: `product_delist:${product.productId}:${currStatus}`,
          title: "Produk Ter-delist!",
          body: `${product.name} status berubah dari NORMAL → ${currStatus}. Cek di Shopee Seller Center.`,
        })
      }
    }
  }

  await Promise.all(upserts)
  return events
}
```

- [ ] **Step 5: Verify build & commit**

```bash
npm run build 2>&1 | tail -10
git add lib/notifications/detectors/
git commit -m "feat(notifications): add detectors for pileup, stock, roas drop, delist"
```

---

## Task 5: Runner

**Files:**
- Create: `lib/notifications/runner.ts`

- [ ] **Step 1: Create runner**

```typescript
// lib/notifications/runner.ts

import { detectOrderPileup } from "./detectors/order-pileup"
import { detectStockLow } from "./detectors/stock-low"
import { detectRoasDrop } from "./detectors/roas-drop"
import { detectProductDelist } from "./detectors/product-delist"
import { wasRecentlySent } from "./dedupe"
import { sendAlert } from "./senders"
import type { AlertEvent, RunnerResult } from "./types"

export async function runAllDetectors(): Promise<RunnerResult> {
  const ranAt = new Date().toISOString()

  // Run detectors in parallel — they're independent.
  // Wrap each in try/catch so one failing detector doesn't kill the run.
  const detectorResults = await Promise.allSettled([
    detectOrderPileup(),
    detectStockLow(),
    detectRoasDrop(),
    detectProductDelist(),
  ])

  const allEvents: AlertEvent[] = []
  for (const r of detectorResults) {
    if (r.status === "fulfilled") {
      allEvents.push(...r.value)
    } else {
      console.warn("Detector failed:", r.reason)
    }
  }

  let sent = 0
  let skipped = 0
  let failed = 0
  const events: RunnerResult["events"] = []

  for (const event of allEvents) {
    const alreadySent = await wasRecentlySent(
      event.alertKey,
      event.dedupHours ?? 24,
    )
    if (alreadySent) {
      skipped++
      events.push({
        alertKey: event.alertKey,
        kind: event.kind,
        sent: false,
        reason: "duplicate within dedup window",
      })
      continue
    }

    const result = await sendAlert(event)
    if (result.anySent) {
      sent++
      events.push({ alertKey: event.alertKey, kind: event.kind, sent: true })
    } else {
      failed++
      events.push({
        alertKey: event.alertKey,
        kind: event.kind,
        sent: false,
        reason: result.results.map((r) => r.error).join("; "),
      })
    }
  }

  return {
    detected: allEvents.length,
    sent,
    skipped,
    failed,
    events,
    ranAt,
  }
}
```

- [ ] **Step 2: Verify build & commit**

```bash
npm run build 2>&1 | tail -10
git add lib/notifications/runner.ts
git commit -m "feat(notifications): add runner that orchestrates all detectors with dedup"
```

---

## Task 6: API Route + Background Polling

**Files:**
- Create: `app/api/notifications/poll/route.ts`
- Create: `instrumentation.ts`

- [ ] **Step 1: API route**

Create `app/api/notifications/poll/route.ts`:

```typescript
import { auth } from "@/lib/auth"
import { runAllDetectors } from "@/lib/notifications/runner"
import { NextRequest, NextResponse } from "next/server"

/**
 * POST /api/notifications/poll
 *
 * Run all detectors once and send any non-deduplicated alerts.
 * Can be called manually from Settings tab (Owner only) or by the
 * background interval started in instrumentation.ts.
 *
 * For manual call: requires Owner session.
 * For background call: instrumentation passes a shared secret in
 * x-internal-secret header.
 */
export async function POST(req: NextRequest) {
  const internalSecret = process.env.INTERNAL_NOTIFICATION_SECRET
  const headerSecret = req.headers.get("x-internal-secret")

  let authorized = false
  if (internalSecret && headerSecret === internalSecret) {
    authorized = true
  } else {
    const session = await auth()
    if (session?.user?.role === "OWNER") {
      authorized = true
    }
  }

  if (!authorized) {
    return NextResponse.json({ error: "Forbidden" }, { status: 403 })
  }

  try {
    const result = await runAllDetectors()
    return NextResponse.json(result)
  } catch (err) {
    const msg = err instanceof Error ? err.message : "Unknown error"
    console.error("POST /api/notifications/poll failed:", err)
    return NextResponse.json({ error: msg }, { status: 500 })
  }
}
```

- [ ] **Step 2: Background polling via instrumentation**

Create `instrumentation.ts` at project root (Next.js convention for startup hooks):

```typescript
// Next.js calls this once when the server starts.
// Used to spin up a background notification poller.

const POLL_INTERVAL_MS = 5 * 60 * 1000 // 5 minutes

export async function register() {
  // Only run on the server, and only when not in build mode
  if (process.env.NEXT_RUNTIME !== "nodejs") return
  if (process.env.NEXT_PHASE === "phase-production-build") return

  console.log("[notifications] Background poller starting...")

  const baseUrl = process.env.NEXTAUTH_URL ?? "http://localhost:3000"
  const secret = process.env.INTERNAL_NOTIFICATION_SECRET

  if (!secret) {
    console.warn(
      "[notifications] INTERNAL_NOTIFICATION_SECRET not set — background poller disabled. Set it in .env.local to enable.",
    )
    return
  }

  // Initial delay so the server is ready before first poll
  setTimeout(() => {
    runPoll(baseUrl, secret)
    setInterval(() => runPoll(baseUrl, secret), POLL_INTERVAL_MS)
  }, 30_000)
}

async function runPoll(baseUrl: string, secret: string) {
  try {
    const res = await fetch(`${baseUrl}/api/notifications/poll`, {
      method: "POST",
      headers: { "x-internal-secret": secret },
    })
    if (!res.ok) {
      console.warn(
        `[notifications] Poll returned ${res.status}: ${await res.text()}`,
      )
      return
    }
    const result = (await res.json()) as {
      detected: number
      sent: number
      skipped: number
      failed: number
    }
    if (result.sent > 0 || result.failed > 0) {
      console.log(
        `[notifications] poll done: detected=${result.detected} sent=${result.sent} skipped=${result.skipped} failed=${result.failed}`,
      )
    }
  } catch (err) {
    console.warn("[notifications] poll error:", err)
  }
}
```

- [ ] **Step 3: Add INTERNAL_NOTIFICATION_SECRET to .env.local template**

User needs to add this manually to `.env.local`:

```
INTERNAL_NOTIFICATION_SECRET=ganti-dengan-string-random-panjang
```

Document this in the task report. The background poller will print a warning if it's not set.

- [ ] **Step 4: Verify build & commit**

```bash
npm run build 2>&1 | tail -10
git add app/api/notifications/ instrumentation.ts
git commit -m "feat(notifications): add poll API route and background poller via instrumentation"
```

---

## Task 7: Notification Runner Card in Settings

**Files:**
- Create: `components/settings/NotificationRunnerCard.tsx`
- Modify: `app/(dashboard)/settings/page.tsx`

- [ ] **Step 1: Runner card component**

Create `components/settings/NotificationRunnerCard.tsx`:

```typescript
"use client"

import { useState } from "react"
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"
import { Button } from "@/components/ui/button"

interface RunnerResult {
  detected: number
  sent: number
  skipped: number
  failed: number
  ranAt: string
  events: Array<{
    alertKey: string
    kind: string
    sent: boolean
    reason?: string
  }>
}

export function NotificationRunnerCard() {
  const [running, setRunning] = useState(false)
  const [lastResult, setLastResult] = useState<RunnerResult | null>(null)
  const [error, setError] = useState<string | null>(null)

  async function handleRun() {
    setRunning(true)
    setError(null)
    try {
      const res = await fetch("/api/notifications/poll", { method: "POST" })
      if (!res.ok) {
        const err = await res.json().catch(() => ({ error: "Unknown error" }))
        throw new Error(err.error ?? `HTTP ${res.status}`)
      }
      const result = (await res.json()) as RunnerResult
      setLastResult(result)
    } catch (err) {
      setError(err instanceof Error ? err.message : "Unknown error")
    } finally {
      setRunning(false)
    }
  }

  return (
    <Card>
      <CardHeader>
        <CardTitle className="text-base">📡 Jalankan Cek Notifikasi</CardTitle>
      </CardHeader>
      <CardContent className="space-y-3">
        <p className="text-xs text-gray-500">
          Background poller jalan otomatis setiap 5 menit (kalau{" "}
          <code>INTERNAL_NOTIFICATION_SECRET</code> ada di .env.local). Tombol
          ini untuk trigger manual.
        </p>
        <Button
          onClick={handleRun}
          disabled={running}
          className="bg-[#EE4D2D] hover:bg-[#d44226] text-white"
        >
          {running ? "Mengecek..." : "Jalankan Sekarang"}
        </Button>

        {error && <div className="text-xs text-red-500">❌ {error}</div>}

        {lastResult && (
          <div className="text-xs text-gray-600 space-y-1">
            <div>
              <strong>Last run:</strong>{" "}
              {new Date(lastResult.ranAt).toLocaleString("id-ID")}
            </div>
            <div>
              Detected: {lastResult.detected} · Sent: {lastResult.sent} ·
              Skipped: {lastResult.skipped} · Failed: {lastResult.failed}
            </div>
            {lastResult.events.length > 0 && (
              <details className="mt-2">
                <summary className="cursor-pointer text-[#EE4D2D]">
                  Detail event ({lastResult.events.length})
                </summary>
                <ul className="mt-1 space-y-0.5 pl-4">
                  {lastResult.events.map((e, i) => (
                    <li key={i}>
                      {e.sent ? "✅" : "⏭"} {e.kind}: {e.alertKey}
                      {e.reason && (
                        <span className="text-gray-400"> ({e.reason})</span>
                      )}
                    </li>
                  ))}
                </ul>
              </details>
            )}
          </div>
        )}
      </CardContent>
    </Card>
  )
}
```

- [ ] **Step 2: Add to settings page**

Edit `app/(dashboard)/settings/page.tsx` — add the runner card after `AlertThresholdCard`:

```typescript
import { NotificationRunnerCard } from "@/components/settings/NotificationRunnerCard"

// ... in the JSX, after AlertThresholdCard:
<NotificationRunnerCard />
```

- [ ] **Step 3: Verify build & commit**

```bash
npm run build 2>&1 | tail -10
git add components/settings/NotificationRunnerCard.tsx "app/(dashboard)/settings/page.tsx"
git commit -m "feat(settings): add notification runner card with manual trigger"
```

---

## Task 8: End-to-End Verification

- [ ] **Step 1: Build + lint clean**

```bash
npm run build 2>&1 | tail -20
npm run lint 2>&1 | tail -10
```

- [ ] **Step 2: Smoke tests**

```bash
npm run start > /tmp/shopee-dash.log 2>&1 &
SERVER_PID=$!
sleep 6

echo "=== Unauth ==="
curl -s -X POST -o /dev/null -w "/api/notifications/poll unauth: %{http_code}\n" \
  http://localhost:3000/api/notifications/poll

echo "=== Wrong secret ==="
curl -s -X POST -H "x-internal-secret: wrong" -o /dev/null -w "%{http_code}\n" \
  http://localhost:3000/api/notifications/poll

kill $SERVER_PID 2>/dev/null
wait 2>/dev/null
```

Expected: 403, 403.

- [ ] **Step 3: Manual test in browser**

Login Owner, masuk tab Settings:

1. **Setup**:
   - Pastikan Telegram & Pushover sudah configured (dari Plan 6)
   - Pastikan threshold sudah diset (default OK)
   - Tambahkan ke `.env.local`:
     ```
     INTERNAL_NOTIFICATION_SECRET=random-string-here
     ```
   - Restart dev server

2. **Manual trigger**:
   - Klik tombol "📡 Jalankan Sekarang" di Notification Runner card
   - Lihat result: detected/sent/skipped/failed
   - Klik "Detail event" untuk lihat event-event yang terdeteksi

3. **Trigger real alert** (mode mock):
   - Aktifkan `SHOPEE_MOCK_PRODUCTS=true` di .env.local
   - Restart dev server
   - Set threshold stok minimum jadi 10 (lebih banyak yang trigger)
   - Klik "Jalankan Sekarang" → harusnya beberapa stock_low events terdeteksi → dikirim ke Telegram + Pushover
   - Cek HP: notif masuk
   - Klik lagi tombol → semua skipped (dedup window 24h)

4. **Background poller**:
   - Restart dev server, lihat console: "[notifications] Background poller starting..."
   - Tunggu 30 detik (initial delay) + 5 menit untuk poll pertama
   - Check console untuk log poll

- [ ] **Step 4: Final report**

Print git log dan summary.

---

## What's NOT in this plan

- **Configurable per-trigger enable/disable** — semua detector aktif kalau channel-nya configured
- **Custom alert templates / branding** — pesan hardcoded, cukup untuk MVP
- **Webhook receiver** (misalnya Shopee push notification ke kita) — defer
- **Email channel** — defer
- **Notification history page** di dashboard — bisa lihat di Prisma Studio aja
- **Rate limiting** Telegram/Pushover API — dedupe sudah cukup mencegah flood
- **Server-Sent Events (SSE) / WebSocket** untuk push real-time ke browser — defer
- **Per-user notification preferences** — config global untuk semua user
