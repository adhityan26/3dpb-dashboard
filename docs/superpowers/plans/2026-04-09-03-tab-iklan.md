# Shopee Dashboard — Plan 3: Tab Iklan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans.

**Goal:** Tab Iklan yang menampilkan performa semua iklan Shopee aktif dan recent — KPI total (ROAS, spend, omzet, jumlah iklan rugi), tabel iklan dengan warna berdasarkan ROAS, sortable kolom, dan rekomendasi otomatis (pause/scale/reactivate).

**Architecture:** Backend ambil data iklan dari Shopee Ads API (`/api/v2/ads/get_all_cpc_ads_hourly_performance` atau `/api/v2/ads/get_all_cpc_ads_daily_performance`), agregasi per iklan untuk range waktu yang dipilih, klasifikasikan warna/rekomendasi di service layer, lalu serve ke frontend yang render tabel interaktif pakai React Query + polling.

**Tech Stack:** Next.js 16, React Query v5, Prisma 7 (untuk cache historis dan tracking state rekomendasi), Shopee Ads API v2.

**Dependencies:** Plan 1 (Foundation), Plan 2 (ada infrastructure yang reusable: `shopeeRequest`, `useRefreshConfig`, `RefreshIndicator`, role guard pattern).

---

## Shopee Ads API Overview

Shopee Open Platform v2 menyediakan beberapa endpoint untuk iklan CPC (dan GMV Max):

| Endpoint | Deskripsi | Catatan |
|----------|-----------|---------|
| `POST /api/v2/ads/get_all_cpc_ads_daily_performance` | Daily performance semua iklan CPC/GMV Max | Range maksimal 30 hari |
| `POST /api/v2/ads/get_all_cpc_ads_hourly_performance` | Hourly performance | Hanya untuk hari yang sedang berjalan |
| `GET /api/v2/ads/get_product_campaign_daily_performance` | Performance per campaign_id | Untuk drilldown spesifik |

**Untuk Plan 3 kita pakai:** `get_all_cpc_ads_daily_performance` untuk periode `7d`/`30d`, dan opsional `hourly` untuk "hari ini".

### Field yang tersedia (per iklan per hari)

| Field | Arti |
|-------|------|
| `campaign_id` | ID unique iklan |
| `campaign_type` | `product` atau `shop` |
| `bidding_method` | `gmv_max_roas`, `gmv_max_auto`, dll |
| `placement_type` | `all`, `homepage`, `search_result` |
| `impression` | Dilihat |
| `clicks` | Jumlah klik |
| `ctr` | Click-through rate |
| `expense` | Biaya iklan (IDR) |
| `direct_order_amount` / `order_amount` | Omzet dari iklan |
| `direct_roi` / `roi` / `broad_gmv` | ROAS |
| `direct_item_sold` / `item_sold` | Qty terjual |
| `cost_per_conversion` | Biaya per konversi |
| `item_id` / `item_name` | Produk yang diiklankan (untuk campaign_type=product) |
| `start_date` / `end_date` | Masa aktif |
| `status` | `berjalan`, `dijeda`, `berakhir` |

**Catatan penting:** Field name asli Shopee pakai `snake_case` dan bisa sedikit beda antar versi API. Implementasi wajib cek respons actual via logging sebelum hardcode mapping.

---

## Klasifikasi ROAS & Rekomendasi

Semua logic ini di-compute di service layer (bukan di UI) biar bisa di-test terpisah dan konsisten antar halaman.

### Warna baris berdasarkan ROAS
| ROAS | Warna | Kategori |
|------|-------|---------|
| ≥ 5x | 🟢 Hijau | Bagus — pertahankan / scale |
| 2–5x | 🟡 Kuning | Sedang — monitor |
| < 2x | 🔴 Merah | Rugi — review urgent |

Threshold ROAS default diset di konstan (`DEFAULT_ROAS_GOOD = 5`, `DEFAULT_ROAS_BAD = 2`). Nanti di Plan 5 (Settings) bisa dibuat configurable lewat `Config` table.

### Rekomendasi otomatis

| Kondisi | Rekomendasi |
|---------|------------|
| Status `berjalan` + ROAS < 1.0 selama 3+ hari berturut-turut | **Pause** — "ROAS di bawah 1x 3 hari — pertimbangkan pause" |
| Status `berjalan` + ROAS ≥ 7.0 + spend < 80% budget harian | **Scale up** — "ROAS tinggi, budget masih cukup — naikkan" |
| Status `dijeda` + historical ROAS ≥ 5.0 (7 hari terakhir sebelum jeda) | **Reactivate** — "Iklan ini ROAS historisnya bagus — pertimbangkan aktifkan" |
| Status `berjalan` + spend > 0 + orders = 0 selama 1+ hari | **Review** — "Iklan spend tapi tidak ada order" |

Rekomendasi tersimpan sebagai computed field per iklan, bukan di DB. Alert/notifikasi baru masuk Plan 5.

---

## File Structure

```
shopee-dashboard/
├── lib/
│   ├── shopee/
│   │   └── ads.ts                 # NEW: getAdsDailyPerformance, getAdsHourlyPerformance
│   ├── ads/
│   │   ├── types.ts               # NEW: AdSummary, AdListResult, AdRecommendation
│   │   ├── classifier.ts          # NEW: pure functions classifyRoas, computeRecommendation
│   │   └── service.ts             # NEW: getAdsPerformance, aggregate per campaign
│   └── hooks/
│       └── use-ads.ts             # NEW: useAds (React Query)
├── app/
│   ├── api/
│   │   └── ads/
│   │       └── route.ts           # NEW: GET /api/ads?range=7d|30d
│   └── (dashboard)/
│       └── iklan/
│           └── page.tsx           # REPLACE: real Ads tab
└── components/
    └── ads/
        ├── AdsKpiBar.tsx          # NEW: 4 KPI cards
        ├── AdsRangeSelector.tsx   # NEW: 7d / 30d toggle
        ├── AdsTable.tsx           # NEW: sortable table
        ├── AdsTableRow.tsx        # NEW: single ad row
        └── AdRecommendationList.tsx  # NEW: rekomendasi summary card
```

---

## Task 1: Shopee Ads API Wrapper

Thin wrapper yang sign dan call endpoint ads Shopee, tanpa aggregation.

**Files:**
- Create: `lib/shopee/ads.ts`
- Modify: `lib/shopee/types.ts` — tambah ads types

- [ ] **Step 1: Tambah types ke `lib/shopee/types.ts`**

Append ke bawah file:

```typescript
// ===== Ads API types =====

export type ShopeeAdStatus = "berjalan" | "dijeda" | "berakhir"
export type ShopeeCampaignType = "product" | "shop"
export type ShopeeBiddingMethod =
  | "gmv_max_roas"
  | "gmv_max_auto"
  | "manual_bidding"
  | "auto_bidding"
  | string // fallback — Shopee may add new methods

export interface ShopeeAdDailyRow {
  campaign_id: number
  campaign_type?: ShopeeCampaignType
  bidding_method?: ShopeeBiddingMethod
  ad_name?: string
  ad_status?: ShopeeAdStatus
  date: string // "YYYY-MM-DD"
  impression?: number
  clicks?: number
  ctr?: number
  expense?: number
  order_amount?: number
  direct_order_amount?: number
  roi?: number
  direct_roi?: number
  broad_roas?: number
  broad_gmv?: number
  item_sold?: number
  direct_item_sold?: number
  item_id?: number
  item_name?: string
}

export interface ShopeeAdsDailyPerformanceResponse {
  response: {
    ad_performance_list?: ShopeeAdDailyRow[]
    // Some API versions use different wrapper names — handle both
    list?: ShopeeAdDailyRow[]
    total?: number
  }
  error?: string
  message?: string
}
```

- [ ] **Step 2: Create `lib/shopee/ads.ts`**

```typescript
import { shopeeRequest } from "./client"
import type {
  ShopeeAdDailyRow,
  ShopeeAdsDailyPerformanceResponse,
} from "./types"

/**
 * Get daily performance for all CPC/GMV-Max ads in a date range.
 * Range max is typically 30 days per Shopee's limits.
 */
export async function getAdsDailyPerformance(params: {
  startDate: string // YYYY-MM-DD
  endDate: string // YYYY-MM-DD
  pageSize?: number
  offset?: number
}): Promise<ShopeeAdDailyRow[]> {
  const query: Record<string, string | number> = {
    start_date: params.startDate,
    end_date: params.endDate,
    page_size: params.pageSize ?? 100,
    offset: params.offset ?? 0,
  }

  const json = await shopeeRequest<ShopeeAdsDailyPerformanceResponse>(
    "/api/v2/ads/get_all_cpc_ads_daily_performance",
    query,
  )

  // Shopee's response wrapper varies — try both known shapes.
  return json.response.ad_performance_list ?? json.response.list ?? []
}

/**
 * Paginate through all results.
 */
export async function getAllAdsDailyPerformance(params: {
  startDate: string
  endDate: string
}): Promise<ShopeeAdDailyRow[]> {
  const all: ShopeeAdDailyRow[] = []
  let offset = 0
  const pageSize = 100
  let safety = 0

  while (safety < 50) {
    const page = await getAdsDailyPerformance({
      startDate: params.startDate,
      endDate: params.endDate,
      pageSize,
      offset,
    })
    all.push(...page)
    if (page.length < pageSize) break
    offset += pageSize
    safety++
  }

  return all
}
```

- [ ] **Step 3: Verify build**

```bash
npm run build 2>&1 | tail -10
```

- [ ] **Step 4: Commit**

```bash
git add lib/shopee/ads.ts lib/shopee/types.ts
git commit -m "feat(shopee): add typed wrapper for ads daily performance endpoint"
```

---

## Task 2: Ads Domain Types & Classifier

Pure functions untuk klasifikasi ROAS dan rekomendasi. Testable tanpa DB atau API.

**Files:**
- Create: `lib/ads/types.ts`
- Create: `lib/ads/classifier.ts`

- [ ] **Step 1: Create types**

Create `lib/ads/types.ts`:

```typescript
export type RoasCategory = "good" | "medium" | "bad"
export type AdStatus = "berjalan" | "dijeda" | "berakhir" | "unknown"

export type AdRecommendationKind =
  | "pause"
  | "scale_up"
  | "reactivate"
  | "review"
  | "none"

export interface AdRecommendation {
  kind: AdRecommendationKind
  reason: string
}

export interface AdSummary {
  campaignId: number
  adName: string
  biddingMethod: string
  campaignType: string
  status: AdStatus
  itemName: string | null
  // aggregated metrics across the date range
  impression: number
  clicks: number
  ctr: number // percent, e.g. 2.45
  expense: number // IDR
  omzet: number // IDR — prefer `order_amount` over `direct_order_amount`
  orders: number
  itemsSold: number
  roas: number // omzet / expense
  acos: number // expense / omzet * 100
  roasCategory: RoasCategory
  recommendation: AdRecommendation
  // per-day series for charts (optional future use)
  daily: Array<{ date: string; expense: number; omzet: number; roas: number }>
}

export interface AdsListResult {
  ads: AdSummary[]
  kpi: {
    totalAds: number
    totalSpend: number
    totalOmzet: number
    totalRoas: number
    adsRugi: number // count where roasCategory === "bad"
    adsGoodCount: number
  }
  range: { startDate: string; endDate: string }
  fetchedAt: string
}
```

- [ ] **Step 2: Create classifier**

Create `lib/ads/classifier.ts`:

```typescript
import type { RoasCategory, AdRecommendation, AdStatus } from "./types"

export const DEFAULT_ROAS_GOOD = 5.0
export const DEFAULT_ROAS_BAD = 2.0

/**
 * Classify a ROAS value into good/medium/bad buckets.
 */
export function classifyRoas(
  roas: number,
  thresholds: { good?: number; bad?: number } = {},
): RoasCategory {
  const good = thresholds.good ?? DEFAULT_ROAS_GOOD
  const bad = thresholds.bad ?? DEFAULT_ROAS_BAD
  if (roas >= good) return "good"
  if (roas < bad) return "bad"
  return "medium"
}

interface RecommendationInput {
  status: AdStatus
  roas: number
  expense: number
  orders: number
  dailyRoas: number[] // sorted oldest → newest
}

/**
 * Compute a single recommendation for an ad based on its aggregated metrics
 * and daily ROAS history.
 */
export function computeRecommendation(
  input: RecommendationInput,
): AdRecommendation {
  const { status, roas, expense, orders, dailyRoas } = input

  // Rule 1: running + ROAS < 1 for 3+ consecutive recent days → pause
  if (status === "berjalan" && dailyRoas.length >= 3) {
    const last3 = dailyRoas.slice(-3)
    if (last3.every((r) => r < 1.0)) {
      return {
        kind: "pause",
        reason: "ROAS di bawah 1x selama 3 hari berturut-turut — pertimbangkan pause",
      }
    }
  }

  // Rule 2: running + spend > 0 + zero orders in the whole range → review
  if (status === "berjalan" && expense > 0 && orders === 0) {
    return {
      kind: "review",
      reason: "Iklan menghabiskan budget tapi tidak ada order",
    }
  }

  // Rule 3: running + ROAS ≥ 7 → scale up suggestion
  if (status === "berjalan" && roas >= 7.0) {
    return {
      kind: "scale_up",
      reason: `ROAS ${roas.toFixed(1)}x — pertimbangkan naikkan budget`,
    }
  }

  // Rule 4: paused + historical ROAS ≥ 5 → reactivate
  if (status === "dijeda" && roas >= 5.0) {
    return {
      kind: "reactivate",
      reason: `ROAS historis ${roas.toFixed(1)}x — pertimbangkan aktifkan kembali`,
    }
  }

  return { kind: "none", reason: "" }
}
```

- [ ] **Step 3: Verify build**

```bash
npm run build 2>&1 | tail -10
```

- [ ] **Step 4: Commit**

```bash
git add lib/ads/types.ts lib/ads/classifier.ts
git commit -m "feat(ads): add domain types and ROAS classifier with recommendations"
```

---

## Task 3: Ads Service — Aggregation

Fetch raw daily rows dari Shopee → agregasi per campaign → compute rekomendasi.

**Files:**
- Create: `lib/ads/service.ts`

- [ ] **Step 1: Create service**

```typescript
import { getAllAdsDailyPerformance } from "@/lib/shopee/ads"
import type { ShopeeAdDailyRow } from "@/lib/shopee/types"
import { classifyRoas, computeRecommendation } from "./classifier"
import type { AdSummary, AdsListResult, AdStatus } from "./types"

export type AdsRange = "7d" | "30d"

function getDateRange(range: AdsRange): { startDate: string; endDate: string } {
  const now = new Date()
  const end = now.toISOString().slice(0, 10) // YYYY-MM-DD
  const days = range === "7d" ? 7 : 30
  const startDate = new Date(now.getTime() - (days - 1) * 24 * 60 * 60 * 1000)
  return { startDate: startDate.toISOString().slice(0, 10), endDate: end }
}

function normalizeStatus(status: string | undefined): AdStatus {
  if (!status) return "unknown"
  const lower = status.toLowerCase()
  if (lower.includes("jalan") || lower === "running") return "berjalan"
  if (lower.includes("jeda") || lower === "paused") return "dijeda"
  if (lower.includes("akhir") || lower === "ended") return "berakhir"
  return "unknown"
}

function firstNumber(...values: Array<number | undefined>): number {
  for (const v of values) {
    if (typeof v === "number" && !Number.isNaN(v)) return v
  }
  return 0
}

/**
 * Fetch ads performance for the given range and aggregate per campaign.
 */
export async function getAdsPerformance(range: AdsRange): Promise<AdsListResult> {
  const { startDate, endDate } = getDateRange(range)
  const rows = await getAllAdsDailyPerformance({ startDate, endDate })

  // Group by campaign_id
  const byCampaign = new Map<number, ShopeeAdDailyRow[]>()
  for (const row of rows) {
    const list = byCampaign.get(row.campaign_id) ?? []
    list.push(row)
    byCampaign.set(row.campaign_id, list)
  }

  const ads: AdSummary[] = []
  for (const [campaignId, campaignRows] of byCampaign.entries()) {
    // Sort by date ascending for daily series
    campaignRows.sort((a, b) => a.date.localeCompare(b.date))

    const expense = campaignRows.reduce((s, r) => s + firstNumber(r.expense), 0)
    const omzet = campaignRows.reduce(
      (s, r) => s + firstNumber(r.order_amount, r.broad_gmv, r.direct_order_amount),
      0,
    )
    const impression = campaignRows.reduce((s, r) => s + firstNumber(r.impression), 0)
    const clicks = campaignRows.reduce((s, r) => s + firstNumber(r.clicks), 0)
    const itemsSold = campaignRows.reduce(
      (s, r) => s + firstNumber(r.item_sold, r.direct_item_sold),
      0,
    )
    const orders = itemsSold // best proxy — Shopee API doesn't always expose unique order count here

    const roas = expense > 0 ? omzet / expense : 0
    const acos = omzet > 0 ? (expense / omzet) * 100 : 0
    const ctr = impression > 0 ? (clicks / impression) * 100 : 0
    const roasCategory = classifyRoas(roas)

    // Meta from the latest row (status may change across days; take newest)
    const latest = campaignRows[campaignRows.length - 1]
    const status = normalizeStatus(latest.ad_status)

    const dailyRoas = campaignRows.map((r) => {
      const dexp = firstNumber(r.expense)
      const domz = firstNumber(r.order_amount, r.broad_gmv, r.direct_order_amount)
      return dexp > 0 ? domz / dexp : 0
    })

    const recommendation = computeRecommendation({
      status,
      roas,
      expense,
      orders,
      dailyRoas,
    })

    ads.push({
      campaignId,
      adName: latest.ad_name ?? `Iklan ${campaignId}`,
      biddingMethod: latest.bidding_method ?? "unknown",
      campaignType: latest.campaign_type ?? "unknown",
      status,
      itemName: latest.item_name ?? null,
      impression,
      clicks,
      ctr,
      expense,
      omzet,
      orders,
      itemsSold,
      roas,
      acos,
      roasCategory,
      recommendation,
      daily: campaignRows.map((r) => {
        const dexp = firstNumber(r.expense)
        const domz = firstNumber(r.order_amount, r.broad_gmv, r.direct_order_amount)
        return {
          date: r.date,
          expense: dexp,
          omzet: domz,
          roas: dexp > 0 ? domz / dexp : 0,
        }
      }),
    })
  }

  // Sort by expense DESC so biggest spenders surface first
  ads.sort((a, b) => b.expense - a.expense)

  const totalSpend = ads.reduce((s, a) => s + a.expense, 0)
  const totalOmzet = ads.reduce((s, a) => s + a.omzet, 0)
  const totalRoas = totalSpend > 0 ? totalOmzet / totalSpend : 0
  const adsRugi = ads.filter((a) => a.roasCategory === "bad").length
  const adsGoodCount = ads.filter((a) => a.roasCategory === "good").length

  return {
    ads,
    kpi: {
      totalAds: ads.length,
      totalSpend,
      totalOmzet,
      totalRoas,
      adsRugi,
      adsGoodCount,
    },
    range: { startDate, endDate },
    fetchedAt: new Date().toISOString(),
  }
}
```

- [ ] **Step 2: Verify build**

```bash
npm run build 2>&1 | tail -10
```

- [ ] **Step 3: Commit**

```bash
git add lib/ads/service.ts
git commit -m "feat(ads): add domain service for aggregating ads performance"
```

---

## Task 4: API Route — GET /api/ads

**Files:**
- Create: `app/api/ads/route.ts`

- [ ] **Step 1: Create route**

```typescript
import { auth } from "@/lib/auth"
import { getAdsPerformance, type AdsRange } from "@/lib/ads/service"
import { NextRequest, NextResponse } from "next/server"

const ALLOWED_ROLES = ["OWNER", "TEST_USER"]

function parseRange(raw: string | null): AdsRange {
  if (raw === "30d") return "30d"
  return "7d"
}

export async function GET(req: NextRequest) {
  const session = await auth()
  if (!session?.user) {
    return NextResponse.json({ error: "Unauthorized" }, { status: 401 })
  }
  if (!ALLOWED_ROLES.includes(session.user.role)) {
    return NextResponse.json({ error: "Forbidden" }, { status: 403 })
  }

  const range = parseRange(req.nextUrl.searchParams.get("range"))

  try {
    const result = await getAdsPerformance(range)
    return NextResponse.json(result)
  } catch (err) {
    const msg = err instanceof Error ? err.message : "Unknown error"
    console.error(`GET /api/ads?range=${range} failed:`, err)
    const notConnected = msg.includes("not authorized") || msg.includes("shop_id not found")
    return NextResponse.json(
      { error: msg, notConnected },
      { status: notConnected ? 503 : 500 },
    )
  }
}
```

- [ ] **Step 2: Verify build**

```bash
npm run build 2>&1 | tail -15
```

- [ ] **Step 3: Commit**

```bash
git add app/api/ads/route.ts
git commit -m "feat(api): add GET /api/ads with range parameter"
```

---

## Task 5: React Query Hook

**Files:**
- Create: `lib/hooks/use-ads.ts`

- [ ] **Step 1: Create hook**

```typescript
"use client"

import { useQuery } from "@tanstack/react-query"
import type { AdsListResult } from "@/lib/ads/types"
import type { AdsRange } from "@/lib/ads/service"
import { useRefreshConfig } from "@/lib/use-refresh-config"

async function fetchAds(range: AdsRange): Promise<AdsListResult> {
  const res = await fetch(`/api/ads?range=${range}`)
  if (!res.ok) {
    const err = await res.json().catch(() => ({ error: "Unknown error" }))
    throw new Error(err.error ?? `HTTP ${res.status}`)
  }
  return res.json()
}

export function useAds(range: AdsRange) {
  const { intervalMs } = useRefreshConfig()
  return useQuery({
    queryKey: ["ads", range] as const,
    queryFn: () => fetchAds(range),
    refetchInterval: intervalMs > 0 ? intervalMs : false,
    refetchIntervalInBackground: false,
  })
}
```

- [ ] **Step 2: Verify build**

```bash
npm run build 2>&1 | tail -10
```

- [ ] **Step 3: Commit**

```bash
git add lib/hooks/use-ads.ts
git commit -m "feat(hooks): add useAds React Query hook"
```

---

## Task 6: Ads Components

Five focused components.

**Files:**
- Create: `components/ads/AdsKpiBar.tsx`
- Create: `components/ads/AdsRangeSelector.tsx`
- Create: `components/ads/AdsTable.tsx`
- Create: `components/ads/AdsTableRow.tsx`
- Create: `components/ads/AdRecommendationList.tsx`

### Step 1: Create `components/ads/AdsKpiBar.tsx`

```typescript
"use client"

import { Card, CardContent } from "@/components/ui/card"
import type { AdsListResult } from "@/lib/ads/types"

interface Props {
  kpi: AdsListResult["kpi"]
}

function fmt(amount: number): string {
  return new Intl.NumberFormat("id-ID", {
    style: "currency",
    currency: "IDR",
    maximumFractionDigits: 0,
  }).format(amount)
}

export function AdsKpiBar({ kpi }: Props) {
  const roasColor =
    kpi.totalRoas >= 5
      ? "text-green-600"
      : kpi.totalRoas < 2
        ? "text-red-600"
        : "text-amber-600"

  return (
    <div className="grid grid-cols-2 md:grid-cols-4 gap-3">
      <Card>
        <CardContent className="p-4">
          <div className="text-xs text-muted-foreground">ROAS Total</div>
          <div className={`text-2xl font-bold ${roasColor}`}>
            {kpi.totalRoas.toFixed(2)}x
          </div>
        </CardContent>
      </Card>
      <Card>
        <CardContent className="p-4">
          <div className="text-xs text-muted-foreground">Total Spend</div>
          <div className="text-2xl font-bold text-blue-600">{fmt(kpi.totalSpend)}</div>
        </CardContent>
      </Card>
      <Card>
        <CardContent className="p-4">
          <div className="text-xs text-muted-foreground">Total Omzet</div>
          <div className="text-2xl font-bold text-[#EE4D2D]">{fmt(kpi.totalOmzet)}</div>
        </CardContent>
      </Card>
      <Card>
        <CardContent className="p-4">
          <div className="text-xs text-muted-foreground">Iklan Rugi</div>
          <div className="text-2xl font-bold text-red-600">
            {kpi.adsRugi}
            <span className="text-sm font-normal text-gray-400">
              {" "}
              / {kpi.totalAds}
            </span>
          </div>
        </CardContent>
      </Card>
    </div>
  )
}
```

### Step 2: Create `components/ads/AdsRangeSelector.tsx`

```typescript
"use client"

import { Button } from "@/components/ui/button"
import type { AdsRange } from "@/lib/ads/service"

interface Props {
  value: AdsRange
  onChange: (value: AdsRange) => void
}

const OPTIONS: Array<{ key: AdsRange; label: string }> = [
  { key: "7d", label: "7 Hari" },
  { key: "30d", label: "30 Hari" },
]

export function AdsRangeSelector({ value, onChange }: Props) {
  return (
    <div className="inline-flex rounded-md border bg-white p-1 gap-1">
      {OPTIONS.map((opt) => {
        const isActive = value === opt.key
        return (
          <Button
            key={opt.key}
            variant={isActive ? "default" : "ghost"}
            size="sm"
            onClick={() => onChange(opt.key)}
            className={
              isActive
                ? "bg-[#EE4D2D] hover:bg-[#d44226] text-white h-8"
                : "h-8"
            }
          >
            {opt.label}
          </Button>
        )
      })}
    </div>
  )
}
```

### Step 3: Create `components/ads/AdsTableRow.tsx`

```typescript
"use client"

import { Badge } from "@/components/ui/badge"
import type { AdSummary } from "@/lib/ads/types"

interface Props {
  ad: AdSummary
}

function fmtIdr(amount: number): string {
  return new Intl.NumberFormat("id-ID", {
    style: "currency",
    currency: "IDR",
    maximumFractionDigits: 0,
  }).format(amount)
}

function fmtNum(n: number): string {
  return new Intl.NumberFormat("id-ID").format(Math.round(n))
}

const ROW_BG: Record<AdSummary["roasCategory"], string> = {
  good: "bg-green-50/50",
  medium: "bg-amber-50/40",
  bad: "bg-red-50/50",
}

const STATUS_COLOR: Record<AdSummary["status"], string> = {
  berjalan: "bg-green-100 text-green-800",
  dijeda: "bg-gray-100 text-gray-700",
  berakhir: "bg-red-100 text-red-800",
  unknown: "bg-gray-100 text-gray-500",
}

const RECOMMENDATION_COLOR: Record<string, string> = {
  pause: "bg-red-100 text-red-800",
  scale_up: "bg-green-100 text-green-800",
  reactivate: "bg-blue-100 text-blue-800",
  review: "bg-amber-100 text-amber-800",
  none: "",
}

export function AdsTableRow({ ad }: Props) {
  const recColor = RECOMMENDATION_COLOR[ad.recommendation.kind]

  return (
    <tr className={`${ROW_BG[ad.roasCategory]} border-b`}>
      <td className="px-3 py-2 text-sm">
        <div className="font-medium truncate max-w-[240px]" title={ad.adName}>
          {ad.adName}
        </div>
        <div className="text-xs text-gray-500">{ad.biddingMethod}</div>
      </td>
      <td className="px-3 py-2">
        <Badge className={STATUS_COLOR[ad.status]}>{ad.status}</Badge>
      </td>
      <td className="px-3 py-2 text-sm text-right">{fmtIdr(ad.expense)}</td>
      <td className="px-3 py-2 text-sm text-right">{fmtIdr(ad.omzet)}</td>
      <td className="px-3 py-2 text-sm text-right font-bold">
        {ad.roas.toFixed(2)}x
      </td>
      <td className="px-3 py-2 text-sm text-right text-gray-600">
        {ad.acos.toFixed(1)}%
      </td>
      <td className="px-3 py-2 text-sm text-right text-gray-600">
        {fmtNum(ad.itemsSold)}
      </td>
      <td className="px-3 py-2">
        {ad.recommendation.kind !== "none" ? (
          <div
            className={`text-xs px-2 py-1 rounded ${recColor}`}
            title={ad.recommendation.reason}
          >
            {ad.recommendation.kind}
          </div>
        ) : (
          <span className="text-xs text-gray-300">—</span>
        )}
      </td>
    </tr>
  )
}
```

### Step 4: Create `components/ads/AdsTable.tsx`

```typescript
"use client"

import { useMemo, useState } from "react"
import type { AdSummary } from "@/lib/ads/types"
import { AdsTableRow } from "./AdsTableRow"

interface Props {
  ads: AdSummary[]
}

type SortKey = "expense" | "omzet" | "roas" | "acos"
type SortDir = "asc" | "desc"

const HEADERS: Array<{ key: SortKey | null; label: string; align?: "right" }> = [
  { key: null, label: "Iklan" },
  { key: null, label: "Status" },
  { key: "expense", label: "Spend", align: "right" },
  { key: "omzet", label: "Omzet", align: "right" },
  { key: "roas", label: "ROAS", align: "right" },
  { key: "acos", label: "ACOS", align: "right" },
  { key: null, label: "Terjual", align: "right" },
  { key: null, label: "Rekomendasi" },
]

export function AdsTable({ ads }: Props) {
  const [sortKey, setSortKey] = useState<SortKey>("expense")
  const [sortDir, setSortDir] = useState<SortDir>("desc")

  const sorted = useMemo(() => {
    const copy = [...ads]
    copy.sort((a, b) => {
      const diff = a[sortKey] - b[sortKey]
      return sortDir === "desc" ? -diff : diff
    })
    return copy
  }, [ads, sortKey, sortDir])

  function toggleSort(key: SortKey | null) {
    if (!key) return
    if (sortKey === key) {
      setSortDir((d) => (d === "desc" ? "asc" : "desc"))
    } else {
      setSortKey(key)
      setSortDir("desc")
    }
  }

  if (ads.length === 0) {
    return (
      <div className="py-12 text-center text-gray-400 text-sm">
        Tidak ada iklan pada periode ini.
      </div>
    )
  }

  return (
    <div className="overflow-x-auto rounded-md border bg-white">
      <table className="min-w-full">
        <thead className="bg-gray-50 text-xs text-gray-600 uppercase">
          <tr>
            {HEADERS.map((h) => (
              <th
                key={h.label}
                className={`px-3 py-2 font-medium ${h.align === "right" ? "text-right" : "text-left"} ${h.key ? "cursor-pointer hover:text-[#EE4D2D]" : ""}`}
                onClick={() => toggleSort(h.key)}
              >
                {h.label}
                {h.key === sortKey && (
                  <span className="ml-1">{sortDir === "desc" ? "↓" : "↑"}</span>
                )}
              </th>
            ))}
          </tr>
        </thead>
        <tbody>
          {sorted.map((ad) => (
            <AdsTableRow key={ad.campaignId} ad={ad} />
          ))}
        </tbody>
      </table>
    </div>
  )
}
```

### Step 5: Create `components/ads/AdRecommendationList.tsx`

```typescript
"use client"

import { Card, CardContent } from "@/components/ui/card"
import type { AdSummary } from "@/lib/ads/types"

interface Props {
  ads: AdSummary[]
}

const KIND_LABEL: Record<string, { title: string; color: string }> = {
  pause: { title: "Stop Dulu", color: "text-red-600" },
  scale_up: { title: "Naikkan Budget", color: "text-green-600" },
  reactivate: { title: "Aktifkan Kembali", color: "text-blue-600" },
  review: { title: "Perlu Review", color: "text-amber-600" },
}

export function AdRecommendationList({ ads }: Props) {
  const withRecs = ads.filter((a) => a.recommendation.kind !== "none")

  if (withRecs.length === 0) {
    return null
  }

  // Group by recommendation kind
  const grouped = new Map<string, AdSummary[]>()
  for (const ad of withRecs) {
    const kind = ad.recommendation.kind
    const list = grouped.get(kind) ?? []
    list.push(ad)
    grouped.set(kind, list)
  }

  return (
    <Card>
      <CardContent className="p-4">
        <h3 className="text-sm font-semibold mb-3">💡 Rekomendasi</h3>
        <div className="space-y-3">
          {Array.from(grouped.entries()).map(([kind, list]) => {
            const meta = KIND_LABEL[kind]
            if (!meta) return null
            return (
              <div key={kind}>
                <div className={`text-xs font-semibold ${meta.color} mb-1`}>
                  {meta.title} ({list.length})
                </div>
                <ul className="space-y-1">
                  {list.slice(0, 5).map((ad) => (
                    <li key={ad.campaignId} className="text-xs text-gray-600">
                      <span className="font-medium">{ad.adName}</span> —{" "}
                      <span className="text-gray-500">{ad.recommendation.reason}</span>
                    </li>
                  ))}
                  {list.length > 5 && (
                    <li className="text-xs text-gray-400">
                      +{list.length - 5} iklan lain
                    </li>
                  )}
                </ul>
              </div>
            )
          })}
        </div>
      </CardContent>
    </Card>
  )
}
```

- [ ] **Step 6: Verify build**

```bash
npm run build 2>&1 | tail -15
```

- [ ] **Step 7: Commit**

```bash
git add components/ads/
git commit -m "feat(ads): add KPI bar, range selector, table, and recommendation components"
```

---

## Task 7: Ads Page

Replace placeholder dengan real implementation.

**Files:**
- Modify: `app/(dashboard)/iklan/page.tsx`

- [ ] **Step 1: Replace page**

```typescript
"use client"

import { useState } from "react"
import { AdsKpiBar } from "@/components/ads/AdsKpiBar"
import { AdsRangeSelector } from "@/components/ads/AdsRangeSelector"
import { AdsTable } from "@/components/ads/AdsTable"
import { AdRecommendationList } from "@/components/ads/AdRecommendationList"
import { RefreshIndicator } from "@/components/layout/RefreshIndicator"
import { useAds } from "@/lib/hooks/use-ads"
import { useRefreshConfig } from "@/lib/use-refresh-config"
import { Button } from "@/components/ui/button"
import type { AdsRange } from "@/lib/ads/service"

export default function IklanPage() {
  const [range, setRange] = useState<AdsRange>("7d")
  const { intervalMs } = useRefreshConfig()
  const { data, isLoading, isError, error, refetch, dataUpdatedAt } = useAds(range)

  if (isLoading && !data) {
    return <div className="py-12 text-center text-gray-400">Memuat iklan...</div>
  }

  if (isError) {
    const msg = error instanceof Error ? error.message : "Unknown error"
    const needsConnect =
      msg.toLowerCase().includes("not authorized") ||
      msg.toLowerCase().includes("shop_id not found")
    return (
      <div className="py-12 text-center space-y-3">
        <div className="text-red-500">{msg}</div>
        {needsConnect && (
          <a
            href="/api/shopee/auth"
            className="inline-flex items-center justify-center h-9 px-4 rounded-md bg-[#EE4D2D] hover:bg-[#d44226] text-white text-sm font-medium"
          >
            Hubungkan Shopee
          </a>
        )}
        <div>
          <Button variant="outline" size="sm" onClick={() => refetch()}>
            Coba lagi
          </Button>
        </div>
      </div>
    )
  }

  if (!data) return null

  return (
    <div className="space-y-4">
      <div className="flex items-center justify-between">
        <h1 className="text-xl font-semibold">Iklan</h1>
        <RefreshIndicator
          lastUpdated={dataUpdatedAt ? new Date(dataUpdatedAt) : null}
          intervalMs={intervalMs}
          onRefresh={() => refetch()}
        />
      </div>

      <div className="flex items-center justify-between">
        <AdsRangeSelector value={range} onChange={setRange} />
        <div className="text-xs text-gray-500">
          {data.range.startDate} – {data.range.endDate}
        </div>
      </div>

      <AdsKpiBar kpi={data.kpi} />

      <AdRecommendationList ads={data.ads} />

      <AdsTable ads={data.ads} />
    </div>
  )
}
```

- [ ] **Step 2: Verify build**

```bash
npm run build 2>&1 | tail -15
```

- [ ] **Step 3: Commit**

```bash
git add "app/(dashboard)/iklan/page.tsx"
git commit -m "feat(iklan): add real Ads tab page with KPI, table, recommendations"
```

---

## Task 8: TabNav Badge — Iklan Rugi

Update dashboard layout untuk populate badge "iklan rugi" di tab Iklan.

**Files:**
- Modify: `app/(dashboard)/layout.tsx`

- [ ] **Step 1: Modify layout**

Replace the `getBadges` function with a version that also fetches ads badge:

```typescript
import { auth } from "@/lib/auth"
import { redirect } from "next/navigation"
import { TabNav } from "@/components/layout/TabNav"
import { countBelumCetak } from "@/lib/orders/service"
import { getAdsPerformance } from "@/lib/ads/service"

async function getBadges(): Promise<Record<string, number>> {
  // Fetch in parallel but tolerate individual failures — badges are optional.
  const [orderResult, adsResult] = await Promise.allSettled([
    countBelumCetak(),
    getAdsPerformance("7d"),
  ])

  const badges: Record<string, number> = {}

  if (orderResult.status === "fulfilled") {
    badges.order = orderResult.value
  } else {
    console.warn("Failed to fetch order badge:", orderResult.reason)
  }

  if (adsResult.status === "fulfilled") {
    badges.iklan = adsResult.value.kpi.adsRugi
  } else {
    console.warn("Failed to fetch ads badge:", adsResult.reason)
  }

  return badges
}

export default async function DashboardLayout({
  children,
}: {
  children: React.ReactNode
}) {
  const session = await auth()
  if (!session?.user) redirect("/login")

  const badges = await getBadges()

  return (
    <div className="min-h-screen bg-gray-50">
      <TabNav role={session.user.role} badges={badges} />
      <main className="max-w-6xl mx-auto p-4">{children}</main>
    </div>
  )
}
```

**Caveat:** Every dashboard navigation now triggers both `countBelumCetak` AND `getAdsPerformance("7d")`. That's heavy. Performance optimization (cache badge counts with TTL) is deferred — not a blocker for Plan 3.

- [ ] **Step 2: Verify build**

```bash
npm run build 2>&1 | tail -15
```

- [ ] **Step 3: Commit**

```bash
git add "app/(dashboard)/layout.tsx"
git commit -m "feat(layout): populate TabNav badge for ads rugi count"
```

---

## Task 9: End-to-End Verification

- [ ] **Step 1: Build + lint clean**

```bash
source ~/.nvm/nvm.sh && nvm use 22 >/dev/null
npm run build 2>&1 | tail -20
npm run lint 2>&1 | tail -10
```

- [ ] **Step 2: Route registered**

Verify `/api/ads` shows up in route listing.

- [ ] **Step 3: Unauthenticated tests**

```bash
npm run start > /tmp/shopee-dash.log 2>&1 &
SERVER_PID=$!
sleep 6

echo "=== Unauth tests ==="
curl -s -o /dev/null -w "/api/ads: %{http_code}\n" http://localhost:3000/api/ads
curl -s -o /dev/null -w "/api/ads?range=30d: %{http_code}\n" "http://localhost:3000/api/ads?range=30d"
curl -s -o /dev/null -w "/iklan: %{http_code}\n" http://localhost:3000/iklan

kill $SERVER_PID 2>/dev/null
wait 2>/dev/null
```

Expected:
- `/api/ads` → 401
- `/api/ads?range=30d` → 401
- `/iklan` → 307 (proxy redirect)

- [ ] **Step 4: Manual browser test (documented, not automated)**

Setelah login sebagai Owner di browser:
1. Klik tab **Iklan**
2. Default range = 7d, data iklan muncul
3. Switch ke 30d → data berubah, range di kanan atas ter-update
4. Badge di tab "Iklan" menunjukkan jumlah iklan rugi (bisa `undefined` kalau tidak ada)
5. Rekomendasi card muncul kalau ada iklan dengan recommendation ≠ none
6. Header kolom bisa di-klik untuk sort (ROAS, spend, omzet, ACOS)
7. Resize ke mobile → KPI cards berubah dari 4 kolom ke 2 kolom, tabel scroll horizontal

---

## What's NOT in this plan

- **Drilldown per iklan** (detail campaign, daily chart) — defer
- **Edit/pause/activate iklan dari dashboard** — monitoring only, no mutation
- **Caching badge counts** — known perf concern, defer to later optimization
- **Configurable ROAS thresholds lewat Settings UI** — defer ke Plan 5 (Settings)
- **Notif saat ROAS turun drastis** — defer ke Plan 5 (Notifikasi)
- **Historical ROAS tracking via DB** — currently only fetches live from Shopee API
- **Per-product ad performance breakdown** — defer
- **Hourly data untuk hari ini** — defer; 7d/30d aja sudah cukup actionable

Cuts ini intentional. Tambah kalau dibutuhkan sebagai follow-up plan.
