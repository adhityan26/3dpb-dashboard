ERRATA: Commit ini pesannya menyesatkan — selain kolom Printer.slug, ia JUGA memuat fix bug produksi saas: chip komponen & labor tak bisa dipilih di http://<IP> karena crypto.randomUUID() undefined di non-secure context. Perubahan komponen (KomponenLaborInput.tsx, SettingsPanel.tsx: crypto.randomUUID → newId) ke-sweep ke sini oleh sesi paralel yang git-add menyeluruh. Helper lib/id.ts + test menyusul di commit 9b781f7. Cari fix crypto lengkap = e9ef2e0 (pemakai) + 9b781f7 (helper).
